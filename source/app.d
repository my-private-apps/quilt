import std.stdio;
import arguments.parser;

void main(string[] arguments) {
	// Create a new argument parser and parse
	// through the command line arguments
	ArgumentParser parser = new ArgumentParser(arguments);
}
